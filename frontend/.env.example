# ===== 前端环境变量示例 =====
# 复制为 .env.development（开发）或 .env.production（生产）后修改

# API 基础路径
# 开发环境: /api（通过 Vite Proxy 代理到后端，见 vite.config.js）
# 生产环境: http://127.0.0.1:8000/api（本地部署，DEC-001 无 CORS）
VITE_API_BASE_URL=/api

# 后端健康检查地址（用于连接状态检测）
VITE_BACKEND_HEALTH_URL=http://127.0.0.1:8000/health
